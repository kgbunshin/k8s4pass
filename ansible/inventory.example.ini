# Copy to inventory.ini (git-ignored) and put your server here.
# bootstrap.yml logs in as root the first time; site.yml switches to the admin user by itself.
[ckad]
your-vps.example.com ansible_user=root
